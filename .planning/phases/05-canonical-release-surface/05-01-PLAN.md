---
phase: 05-canonical-release-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - V2/release-manifest.json
  - scripts/package_review_gate_vsix.py
  - V2/cursor-extension/package.json
  - .gitignore
  - V2/cursor-extension/review-gate-v2-2.7.3.vsix
autonomous: true
requirements:
  - REL-01
user_setup: []
must_haves:
  truths:
    - "Maintainer has one canonical release manifest and one packaging command that produce the shipped `V2/review-gate-v2-2.7.3.vsix` artifact."
    - "The committed `V2/cursor-extension/` build-output VSIX no longer exists as a second supported release artifact."
    - "Release metadata for version, artifact basename, and supported rule file live in one repo-owned source of truth instead of drifting across scripts and docs."
  artifacts:
    - path: V2/release-manifest.json
      provides: "Single source of truth for Review Gate release metadata including version, canonical VSIX path, and supported rule file."
      contains: "artifact_basename"
    - path: scripts/package_review_gate_vsix.py
      provides: "Canonical packaging helper that reads the release manifest, validates package metadata, and emits the VSIX into `V2/`."
      contains: "canonical_vsix_path"
    - path: V2/cursor-extension/package.json
      provides: "Extension packaging entrypoint that delegates `npm run package` to the canonical packaging helper instead of default local output."
      contains: "\"package\""
    - path: .gitignore
      provides: "Guardrail that keeps generated `V2/cursor-extension/*.vsix` outputs out of the committed release surface."
      contains: "V2/cursor-extension/*.vsix"
  key_links:
    - from: V2/release-manifest.json
      to: scripts/package_review_gate_vsix.py
      via: "helper parses manifest fields to resolve the only supported artifact path and rule file"
      pattern: "artifact_basename|canonical_vsix_path|rule_path"
    - from: V2/cursor-extension/package.json
      to: scripts/package_review_gate_vsix.py
      via: "`npm run package` delegates packaging to the repo-level helper"
      pattern: "package_review_gate_vsix.py"
    - from: scripts/package_review_gate_vsix.py
      to: V2/review-gate-v2-2.7.3.vsix
      via: "helper writes or validates the canonical root-level VSIX instead of `cursor-extension/` output"
      pattern: "review-gate-v2-2\\.7\\.3\\.vsix"
---

<objective>
Establish one canonical VSIX production contract for Review Gate V2.

Purpose: Phase 1 aligned the current filenames, but the repo still ships two identical committed VSIX files. This plan removes the second committed artifact surface and gives maintainers one authoritative packaging path for the supported V2 runtime.
Output: A release manifest, a canonical VSIX packaging helper wired to `npm run package`, and removal of the duplicate committed `cursor-extension` VSIX.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-canonical-release-surface/05-CONTEXT.md
@.planning/phases/01-installation-integrity/01-03-SUMMARY.md
@.planning/phases/04-automated-hardening-verification/04-03-SUMMARY.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@V2/cursor-extension/package.json
@V2/review-gate-v2-2.7.3.vsix
@V2/cursor-extension/review-gate-v2-2.7.3.vsix

<interfaces>
From `V2/cursor-extension/package.json`:
```json
{
  "name": "review-gate-v2",
  "version": "2.7.3",
  "scripts": {
    "package": "vsce package"
  }
}
```

Current duplicate artifact state:
```text
V2/review-gate-v2-2.7.3.vsix
V2/cursor-extension/review-gate-v2-2.7.3.vsix
sha256: both files currently match
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a canonical release manifest and packaging helper</name>
  <files>V2/release-manifest.json, scripts/package_review_gate_vsix.py, V2/cursor-extension/package.json</files>
  <read_first>V2/cursor-extension/package.json, V2/review-gate-v2-2.7.3.vsix, .planning/phases/04-automated-hardening-verification/04-03-SUMMARY.md</read_first>
  <action>Create `V2/release-manifest.json` as the single source of truth for the supported V2 release surface. Include at least the current version `2.7.3`, the canonical VSIX basename and repo path under `V2/`, the supported rule file `V2/ReviewGateV2.mdc`, and the extension source directory. Add `scripts/package_review_gate_vsix.py` using only Python stdlib (`json`, `subprocess`, `zipfile`, `pathlib`, `argparse`) so maintainers can: 1) print manifest fields for downstream scripts, 2) verify manifest and `V2/cursor-extension/package.json` stay in sync, and 3) package the extension into `V2/review-gate-v2-<version>.vsix` instead of writing a second supported artifact under `V2/cursor-extension/`. Update `V2/cursor-extension/package.json` so `npm run package` delegates to this helper. Do not add marketplace publishing, GitHub Releases automation, or CI-only packaging steps in this phase.</action>
  <verify>python3 scripts/package_review_gate_vsix.py --check</verify>
  <acceptance_criteria>
    - The manifest is the only repo-owned place that defines the supported version, canonical VSIX path, and supported rule path.
    - `npm run package` from `V2/cursor-extension/` writes the release artifact into `V2/` and exposes machine-readable metadata for downstream scripts.
  </acceptance_criteria>
  <done>Maintainers have one authoritative release metadata contract and one canonical packaging command for the shipped VSIX.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the duplicate committed build-output VSIX and keep it untracked</name>
  <files>.gitignore, V2/cursor-extension/review-gate-v2-2.7.3.vsix</files>
  <read_first>.gitignore, V2/cursor-extension/review-gate-v2-2.7.3.vsix, V2/review-gate-v2-2.7.3.vsix</read_first>
  <action>Delete the duplicate committed artifact `V2/cursor-extension/review-gate-v2-2.7.3.vsix`. Add a `.gitignore` rule that keeps generated `V2/cursor-extension/*.vsix` files out of source control so the extension workspace can remain a build directory without becoming a second release surface again. Keep the canonical committed root artifact `V2/review-gate-v2-2.7.3.vsix` in place for this phase. Do not delete the root artifact or leave the repo with zero committed authoritative VSIX files.</action>
  <verify>test ! -e V2/cursor-extension/review-gate-v2-2.7.3.vsix && test -e V2/review-gate-v2-2.7.3.vsix && git check-ignore -q V2/cursor-extension/review-gate-v2-2.7.3.vsix</verify>
  <acceptance_criteria>
    - The repo contains one committed authoritative VSIX under `V2/`.
    - Generated `V2/cursor-extension/*.vsix` files stay untracked instead of becoming a second committed release path.
  </acceptance_criteria>
  <done>The repository no longer presents `V2/cursor-extension/` as a second committed VSIX surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 scripts/package_review_gate_vsix.py --check`
- [ ] `test ! -e V2/cursor-extension/review-gate-v2-2.7.3.vsix`
- [ ] `test -e V2/review-gate-v2-2.7.3.vsix`
- [ ] `git check-ignore -q V2/cursor-extension/review-gate-v2-2.7.3.vsix`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo has one canonical committed VSIX path under `V2/`
- Maintainers can package the supported artifact through one helper-backed command without recreating a second committed surface
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-release-surface/05-01-SUMMARY.md`
</output>
