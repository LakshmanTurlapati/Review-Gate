---
phase: 05-canonical-release-surface
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - V2/INSTALLATION.md
  - readme.md
  - tests/python/test_release_surface.py
  - scripts/run_review_gate_regression_checks.py
autonomous: true
requirements:
  - REL-01
user_setup: []
must_haves:
  truths:
    - "Installation and release docs describe one authoritative Review Gate artifact path, one supported rule file, and one supported packaging command."
    - "Maintainers can run the repo-root regression runner to verify the narrowed release surface before shipping."
    - "Users and maintainers no longer see `V2/cursor-extension/` described as a co-equal supported release artifact."
  artifacts:
    - path: V2/INSTALLATION.md
      provides: "Detailed install and release-facing guidance aligned to the canonical root VSIX and `ReviewGateV2.mdc`."
      contains: "V2/review-gate-v2-2.7.3.vsix"
    - path: readme.md
      provides: "Top-level public guidance that points users to one authoritative V2 artifact story."
      contains: "V2/review-gate-v2-2.7.3.vsix"
    - path: tests/python/test_release_surface.py
      provides: "Release-surface regression suite that checks manifest, docs, installers, and packaged VSIX metadata alignment."
      contains: "zipfile"
    - path: scripts/run_review_gate_regression_checks.py
      provides: "Repo-root regression runner extended with a release-surface suite and included in the default release check."
      contains: "release-surface"
  key_links:
    - from: V2/release-manifest.json
      to: V2/INSTALLATION.md
      via: "docs name the canonical artifact basename, supported rule path, and packaging command from the shared release contract"
      pattern: "review-gate-v2-2\\.7\\.3\\.vsix|ReviewGateV2\\.mdc"
    - from: V2/release-manifest.json
      to: tests/python/test_release_surface.py
      via: "release regression suite loads manifest JSON and compares it to docs, installers, and VSIX package metadata"
      pattern: "artifact_basename|canonical_vsix_path|rule_path"
    - from: tests/python/test_release_surface.py
      to: scripts/run_review_gate_regression_checks.py
      via: "runner exposes and executes the new release-surface suite as part of the full repo-root regression command"
      pattern: "release-surface"
---

<objective>
Align public release guidance and add a final regression gate for the canonical release surface.

Purpose: After Plans 05-01 and 05-02 establish the canonical artifact contract, this plan makes the docs authoritative and adds automated proof that the manifest, installers, docs, and shipped VSIX still agree before release.
Output: Updated public docs plus a release-surface regression suite wired into the existing repo-root runner.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-canonical-release-surface/05-CONTEXT.md
@.planning/phases/01-installation-integrity/01-04-SUMMARY.md
@.planning/phases/04-automated-hardening-verification/04-03-SUMMARY.md
@V2/release-manifest.json
@V2/INSTALLATION.md
@readme.md
@V2/install.sh
@V2/install.ps1
@V2/install.bat
@V2/cursor-extension/package.json
@V2/review-gate-v2-2.7.3.vsix
@scripts/run_review_gate_regression_checks.py

<interfaces>
Current runner suites from `scripts/run_review_gate_regression_checks.py`:
```python
[
  "python-server",
  "node-extension",
  "installers",
]
```

Expected new regression contract for this plan:
```text
- manifest version matches V2/cursor-extension/package.json
- canonical root VSIX exists and contains matching extension/package.json metadata
- V2/cursor-extension/review-gate-v2-2.7.3.vsix is absent
- docs and installers reference V2/review-gate-v2-2.7.3.vsix and V2/ReviewGateV2.mdc only
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite public release guidance around one authoritative artifact story</name>
  <files>V2/INSTALLATION.md, readme.md</files>
  <read_first>V2/INSTALLATION.md, readme.md, V2/release-manifest.json, V2/install.sh, V2/install.ps1, V2/install.bat</read_first>
  <action>Update `V2/INSTALLATION.md` and `readme.md` so they describe one authoritative V2 release surface: the canonical committed VSIX `V2/review-gate-v2-2.7.3.vsix`, the supported rule file `V2/ReviewGateV2.mdc`, and the canonical packaging command introduced in 05-01. Remove language that frames `V2/cursor-extension/` as a supported fallback artifact location or an equal install choice. Keep the V1 legacy section only where it does not contradict the V2 release story. Do not add marketplace publishing, CI release pipeline steps, or broader distribution changes that were explicitly deferred.</action>
  <verify>! rg -n 'cursor-extension/review-gate-v2-.*\\.vsix|fallback build-output|co-equal artifact' V2/INSTALLATION.md readme.md && rg -n 'V2/review-gate-v2-2\\.7\\.3\\.vsix|V2/ReviewGateV2\\.mdc|npm run package|package_review_gate_vsix\\.py' V2/INSTALLATION.md readme.md</verify>
  <acceptance_criteria>
    - Public docs point to one supported VSIX path under `V2/`.
    - Public docs still reflect the actual installer and rule behavior shipped after 05-02.
  </acceptance_criteria>
  <done>Users and maintainers can read the docs without guessing which artifact or rule path is actually supported.</done>
</task>

<task type="auto">
  <name>Task 2: Add release-surface regression checks and wire them into the repo-root runner</name>
  <files>tests/python/test_release_surface.py, scripts/run_review_gate_regression_checks.py</files>
  <read_first>V2/release-manifest.json, V2/cursor-extension/package.json, V2/review-gate-v2-2.7.3.vsix, V2/install.sh, V2/install.ps1, V2/install.bat, scripts/run_review_gate_regression_checks.py</read_first>
  <action>Create `tests/python/test_release_surface.py` using only Python stdlib (`unittest`, `json`, `zipfile`, `pathlib`, `hashlib` if helpful). The suite must assert the canonical release contract end to end: manifest version and basename match `V2/cursor-extension/package.json`; the canonical root VSIX exists and its embedded `extension/package.json` matches the source manifest version; the duplicate `V2/cursor-extension/review-gate-v2-2.7.3.vsix` is absent; installers reference the canonical artifact and `ReviewGateV2.mdc` only; and the public docs describe the same narrowed surface. Then extend `scripts/run_review_gate_regression_checks.py` with a `release-surface` suite and include it in the default run after the existing Phase 04 suites so maintainers reuse the same repo-root command. Do not create a second top-level release-check script or add CI workflow files in this phase.</action>
  <verify>python3 -m unittest tests/python/test_release_surface.py -v && python3 scripts/run_review_gate_regression_checks.py --suite release-surface</verify>
  <acceptance_criteria>
    - A dedicated regression suite proves docs, installers, manifest, and canonical VSIX metadata still agree.
    - The existing repo-root runner is the single release-check entrypoint and includes the new release-surface suite by default.
  </acceptance_criteria>
  <done>Maintainers can run one repo-root regression command that proves the narrowed release surface before shipping.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests/python/test_release_surface.py -v`
- [ ] `python3 scripts/run_review_gate_regression_checks.py --suite release-surface`
- [ ] `python3 scripts/run_review_gate_regression_checks.py`
- [ ] Confirm docs and installer references no longer describe `V2/cursor-extension/` as a supported artifact surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Public docs, installers, manifest, and canonical VSIX metadata agree on one supported Review Gate release surface
- Maintainers can prove that alignment through the existing repo-root regression runner before release
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-release-surface/05-03-SUMMARY.md`
</output>
