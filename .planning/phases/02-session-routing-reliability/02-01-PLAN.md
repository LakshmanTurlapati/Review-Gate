---
phase: 02-session-routing-reliability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - V2/review_gate_v2_mcp.py
  - V2/cursor-extension/extension.js
autonomous: true
requirements:
  - SESS-01
  - SESS-02
user_setup: []
must_haves:
  truths:
    - "A Review Gate popup round-trip keeps trigger, acknowledgement, and response ownership tied to the active session instead of a shared temp-file slot."
    - "Cursor receives user input only from the matching active-session response, with no stale or generic fallback content accepted."
    - "A later trigger cannot silently rebind an in-progress popup away from the session the user is answering."
  artifacts:
    - path: V2/review_gate_v2_mcp.py
      provides: "Canonical session file helpers plus session-scoped trigger and response handling for `review_gate_chat`."
      contains: "_session_file"
    - path: V2/cursor-extension/extension.js
      provides: "Canonical session file helpers plus explicit active-session state for MCP popup routing."
      contains: "activeMcpSession"
  key_links:
    - from: V2/review_gate_v2_mcp.py
      to: V2/cursor-extension/extension.js
      via: "session-scoped trigger files named from the same `trigger_id`"
      pattern: "review_gate_trigger_"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "single authoritative response file `review_gate_response_<trigger_id>.json`"
      pattern: "review_gate_response_"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "matching acknowledgement file `review_gate_ack_<trigger_id>.json`"
      pattern: "review_gate_ack_"
---

<objective>
Make the temp-file IPC contract deterministic so the active Review Gate exchange is routed only through session-owned files.

Purpose: Remove the current overwrite and stale-fallback paths before adding failure recovery on top of them.
Output: Updated Python and extension runtimes that use one authoritative trigger, acknowledgement, and response path per `trigger_id`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-routing-reliability/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@V2/review_gate_v2_mcp.py
@V2/cursor-extension/extension.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish one canonical session file contract in both runtimes</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js, .planning/phases/02-session-routing-reliability/02-CONTEXT.md</read_first>
  <action>At the top of both runtime files, define matching helpers for the exact session-scoped artifacts `review_gate_trigger_<trigger_id>.json`, `review_gate_ack_<trigger_id>.json`, `review_gate_response_<trigger_id>.json`, `review_gate_speech_trigger_<trigger_id>.json`, and `review_gate_speech_response_<trigger_id>.json`. Use `_session_file(kind, trigger_id)` in Python and `getSessionFilePath(kind, triggerId)` plus `listPendingTriggerFiles()` in JavaScript or equivalent exact helpers so later tasks stop scattering filename literals. Remove hardcoded generic response filenames and backup trigger filenames from the active MCP path. Do not move storage out of the current OS temp directory, add auth tokens, or introduce a queued multi-popup UX in this task.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && python3 - <<'PY'
from pathlib import Path
text = Path('V2/review_gate_v2_mcp.py').read_text(encoding='utf-8') + "\n" + Path('V2/cursor-extension/extension.js').read_text(encoding='utf-8')
for required in ['_session_file', 'getSessionFilePath', 'listPendingTriggerFiles']:
    assert required in text, required
for forbidden in [
    'review_gate_response.json',
    'mcp_response.json',
    'review_gate_trigger_0.json',
    'review_gate_trigger_1.json',
    'review_gate_trigger_2.json',
]:
    assert forbidden not in text, forbidden
print('ok')
PY</verify>
  <acceptance_criteria>
    - Both runtimes derive trigger, ack, response, and speech filenames from shared helper conventions instead of repeated inline strings.
    - No active-flow literals remain for `review_gate_response.json`, `mcp_response.json`, or numbered backup trigger files.
  </acceptance_criteria>
  <done>The Python server and Cursor extension now agree on one session-scoped temp-file contract for active MCP exchanges.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the Python server to use only the owning session's trigger and response files</name>
  <files>V2/review_gate_v2_mcp.py</files>
  <read_first>V2/review_gate_v2_mcp.py, .planning/codebase/INTEGRATIONS.md</read_first>
  <action>Refactor `_trigger_cursor_popup_immediately`, `_wait_for_extension_acknowledgement`, `_wait_for_user_input`, and `_handle_get_user_input` so `review_gate_chat` writes exactly one trigger file for the current `trigger_id`, waits only on the matching acknowledgement and response files, and skips plus cleans mismatched session envelopes instead of accepting them. Delete `_create_backup_triggers` and remove the alternate `mcp_response_<trigger_id>.json` compatibility path from the active MCP chat flow. Keep the public tool name `review_gate_chat`, the existing 5-minute default response timeout, and the current attachment return behavior. Ensure `_last_attachments` is cleared on timeout or any non-response outcome.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && ! rg -n '_create_backup_triggers|mcp_response_|review_gate_response\\.json|review_gate_trigger_[0-2]\\.json' V2/review_gate_v2_mcp.py</verify>
  <acceptance_criteria>
    - `review_gate_chat` creates one session trigger file instead of one shared trigger plus numbered backups.
    - The Python wait loops no longer poll `review_gate_response.json` or `mcp_response_<id>.json`.
    - Attachment state is reset when a session does not end with a valid user response.
  </acceptance_criteria>
  <done>The MCP server routes active popup requests exclusively through the matching session files and cannot accept stale fallback responses.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor the extension watcher and popup routing around one active MCP session</name>
  <files>V2/cursor-extension/extension.js</files>
  <read_first>V2/cursor-extension/extension.js, .planning/codebase/CONCERNS.md</read_first>
  <action>Replace passive `currentTriggerData` overwrites with explicit `activeMcpSession` and `handledTriggerIds` state. Poll pending `review_gate_trigger_*.json` files from the temp directory, process them in deterministic order, and ignore already-handled trigger IDs so the same session is not reopened repeatedly. When the user submits an MCP reply, write exactly one authoritative `review_gate_response_<trigger_id>.json` file through a dedicated `writeSessionResponse` helper. Preserve the current single popup panel and the manual `reviewGate.openChat` flow; do not queue later sessions in this plan.</action>
  <verify>node --check V2/cursor-extension/extension.js && rg -n 'activeMcpSession|handledTriggerIds|writeSessionResponse|listPendingTriggerFiles' V2/cursor-extension/extension.js && ! rg -n 'review_gate_response\\.json|mcp_response_|review_gate_trigger_[0-2]\\.json' V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - The extension has explicit active-session state instead of relying on silent `currentTriggerData` replacement.
    - The extension writes one session-scoped response file per MCP submission and no generic fallback response files.
    - Trigger polling is driven by discovered session files rather than a shared trigger filename plus numbered backups.
  </acceptance_criteria>
  <done>The extension can no longer silently repoint an in-progress popup reply to generic fallback files or a shared trigger slot.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile V2/review_gate_v2_mcp.py`
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `! rg -n 'review_gate_response\\.json|mcp_response_|review_gate_trigger_[0-2]\\.json' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js`
- [ ] Confirm both runtimes derive trigger, ack, response, and speech filenames from session-scoped helpers rather than repeated literals.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The active MCP popup flow uses only session-scoped trigger, acknowledgement, and response files, satisfying the routing isolation required for SESS-01 and SESS-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-routing-reliability/02-01-SUMMARY.md`
</output>
