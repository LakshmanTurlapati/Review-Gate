---
phase: 04-automated-hardening-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/python/review_gate_test_loader.py
  - tests/python/test_review_gate_v2_mcp.py
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "Maintainer can run a Python-only regression suite that exercises trigger creation, acknowledgement handling, response matching, and timeout behavior without launching Cursor."
    - "Busy, cancelled, malformed, and wrong-session envelopes are covered by automated checks instead of only manual runtime probing."
    - "Timeout and non-response outcomes are verified to clear attachment state and leave the server on the documented session contract from Phases 2 and 3."
  artifacts:
    - path: tests/python/review_gate_test_loader.py
      provides: "Stdlib-only loader that imports `V2/review_gate_v2_mcp.py` with stubbed MCP and Whisper dependencies plus isolated runtime ids."
      contains: "load_review_gate_module"
    - path: tests/python/test_review_gate_v2_mcp.py
      provides: "Regression coverage for trigger proof generation, acknowledgement outcomes, response matching, and timeout cleanup."
      contains: "ReviewGateServer"
  key_links:
    - from: tests/python/review_gate_test_loader.py
      to: V2/review_gate_v2_mcp.py
      via: "stubbed module import that keeps unit tests off the real MCP runtime and speech model"
      pattern: "importlib|sys\\.modules|ReviewGateServer"
    - from: tests/python/test_review_gate_v2_mcp.py
      to: V2/review_gate_v2_mcp.py
      via: "session-scoped trigger, acknowledgement, and response files written into the same runtime root used by the server helpers"
      pattern: "_trigger_cursor_popup_immediately|_wait_for_extension_acknowledgement|_wait_for_user_input"
---

<objective>
Add a dependency-light Python regression harness for the shipped MCP server behavior.

Purpose: Give maintainers a repeatable way to verify the hardened server contract from Phases 2 and 3 before release without needing a live Cursor popup.
Output: Stdlib-only Python tests that import the server safely, write real session envelopes into isolated runtime directories, and assert trigger, ack, response, and timeout behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automated-hardening-verification/04-CONTEXT.md
@.planning/phases/02-session-routing-reliability/02-01-SUMMARY.md
@.planning/phases/02-session-routing-reliability/02-02-SUMMARY.md
@.planning/phases/03-scoped-ipc-security/03-02-SUMMARY.md
@.planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@V2/review_gate_v2_mcp.py

<interfaces>
From `V2/review_gate_v2_mcp.py`:
```python
class ReviewGateServer:
    def _build_initial_trigger_signature(
        self,
        *,
        trigger_id: str,
        protocol_version: str,
        session_token: str,
        timestamp: str,
        pid: int,
        tool_name: str,
    ) -> str: ...

    def _validate_session_envelope(
        self,
        envelope: Any,
        *,
        expected_trigger_id: Optional[str] = None,
        expected_source: Optional[str] = None,
        required_data_keys: Optional[Sequence[str]] = None,
    ) -> Dict[str, Any]: ...

    async def _wait_for_extension_acknowledgement(self, trigger_id: str, timeout: int = 30) -> bool: ...
    async def _wait_for_user_input(self, trigger_id: str, timeout: int = 120) -> Optional[str]: ...
    async def _trigger_cursor_popup_immediately(self, data: dict) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a stdlib-only loader for importing the Review Gate server in tests</name>
  <files>tests/python/review_gate_test_loader.py</files>
  <read_first>V2/review_gate_v2_mcp.py, .planning/phases/03-scoped-ipc-security/03-02-SUMMARY.md, .planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md</read_first>
  <action>Create `tests/python/review_gate_test_loader.py` as the only import seam for Python tests. Stub `mcp.server`, `mcp.server.models`, `mcp.server.stdio`, `mcp.types`, and `faster_whisper` in `sys.modules`, then load `V2/review_gate_v2_mcp.py` with `importlib` so tests can import the shipped module without a venv or model download. The loader must also expose helpers for unique `REVIEW_GATE_USER_ID` values and for monkeypatching `ReviewGateServer._start_speech_monitoring` during tests so no background speech thread survives the test process. Use only Python stdlib (`importlib`, `types`, `tempfile`, `unittest.mock`). Do not add pytest, install-time dependencies, or production-only test branches in `V2/review_gate_v2_mcp.py` if the loader can avoid them.</action>
  <verify>python3 - <<'PY'
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path('tests/python').resolve()))
from review_gate_test_loader import load_review_gate_module

module = load_review_gate_module()
assert hasattr(module, 'ReviewGateServer')
assert hasattr(module, 'IPC_PROTOCOL_VERSION')
print('ok')
PY</verify>
  <acceptance_criteria>
    - The server module can be imported in tests without installing `mcp` or `faster-whisper`.
    - The loader provides deterministic runtime isolation and disables background speech monitoring for unit tests.
  </acceptance_criteria>
  <done>Python tests have a single supported loader for importing the shipped server module without external runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for trigger proof, acknowledgement outcomes, response matching, and timeout cleanup</name>
  <files>tests/python/test_review_gate_v2_mcp.py</files>
  <read_first>tests/python/review_gate_test_loader.py, V2/review_gate_v2_mcp.py, .planning/phases/02-session-routing-reliability/02-02-SUMMARY.md, .planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md</read_first>
  <action>Add `tests/python/test_review_gate_v2_mcp.py` with `unittest` and `IsolatedAsyncioTestCase`. Cover the current shipped server contract, not legacy fallback paths: `review_gate_trigger_<trigger_id>.json` creation with `protocol_version`, `session_token`, `trigger_signature`, and `trigger_issued_at`; acknowledgement handling for matching `acknowledged`, `busy`, `cancelled`, and malformed envelopes; response matching that accepts only the owning trigger/session token, carries attachment descriptions into the returned text, and ignores or removes wrong-session files; and timeout branches that clear `_last_attachments` plus leave `_last_session_outcome` or `_last_acknowledgement_outcome` consistent with the hardened runtime behavior. Drive the wait loops by writing real session files into isolated runtime directories with short async helpers; do not launch the full MCP server or real speech monitor thread.</action>
  <verify>python3 -m unittest discover -s tests/python -p 'test_review_gate_v2_mcp.py' -v</verify>
  <acceptance_criteria>
    - The suite covers trigger creation, acknowledgement handling, response matching, and timeout cleanup for `ReviewGateServer`.
    - Wrong-session, malformed, busy, and cancelled envelopes are asserted directly against the shipped session-envelope contract.
  </acceptance_criteria>
  <done>Maintainers can run automated Python regression checks for the server behaviors required by QUAL-01.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 - <<'PY' ... load_review_gate_module() ... PY`
- [ ] `python3 -m unittest discover -s tests/python -p 'test_review_gate_v2_mcp.py' -v`
- [ ] Confirm the suite never depends on a real Cursor process, real `mcp` install, or Faster-Whisper model download.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No heavy test dependency or CI-specific framework was introduced
- Maintainers can run a repo-local Python regression suite that proves the shipped server still satisfies QUAL-01.
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-hardening-verification/04-01-SUMMARY.md`
</output>
