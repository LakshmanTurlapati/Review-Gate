---
phase: 04-automated-hardening-verification
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - V2/cursor-extension/extension.js
  - tests/node/load-extension.js
  - tests/node/extension.runtime.test.js
autonomous: true
requirements:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - "Maintainer can run Node-based extension tests that validate trigger intake, popup lifecycle, session routing, and attachment handling without launching Cursor."
    - "A second trigger during an active popup, a disposed popup, and an uploaded attachment are all covered by automated extension checks."
    - "The extension exposes only the minimal deterministic test surface required for repo-local regression harnesses, without introducing a new runtime architecture."
  artifacts:
    - path: V2/cursor-extension/extension.js
      provides: "A minimal `__testHooks` export or equivalent deterministic seam for loading the extension under a stubbed VS Code API."
      contains: "__testHooks"
    - path: tests/node/load-extension.js
      provides: "A built-in-only loader that stubs the `vscode` module, fake panels, and fake output channels before importing the shipped extension."
      contains: "loadExtensionForTest"
    - path: tests/node/extension.runtime.test.js
      provides: "Node `node:test` coverage for trigger proof validation, busy-session routing, popup cancellation, and attachment response writing."
      contains: "test("
  key_links:
    - from: tests/node/load-extension.js
      to: V2/cursor-extension/extension.js
      via: "stubbed `vscode` module and fake webview panel state"
      pattern: "Module\\._load|createWebviewPanel|showOpenDialog"
    - from: tests/node/extension.runtime.test.js
      to: V2/cursor-extension/extension.js
      via: "test hooks that drive `processTriggerFile`, popup creation, result writing, and attachment upload handling"
      pattern: "processTriggerFile|openReviewGatePopup|writeSessionResponse|handleImageUpload"
---

<objective>
Add a dependency-light Node regression harness for the shipped Cursor extension behavior.

Purpose: Give maintainers an automated way to validate the hardened extension/session contract from Phases 2 and 3 without real Cursor UI automation.
Output: Built-in `node:test` coverage that loads the single-file extension under a stubbed VS Code API and asserts popup lifecycle, session routing, and attachment behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automated-hardening-verification/04-CONTEXT.md
@.planning/phases/02-session-routing-reliability/02-01-SUMMARY.md
@.planning/phases/02-session-routing-reliability/02-02-SUMMARY.md
@.planning/phases/03-scoped-ipc-security/03-03-SUMMARY.md
@.planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@V2/cursor-extension/extension.js
@V2/cursor-extension/package.json

<interfaces>
From `V2/cursor-extension/extension.js`:
```javascript
function validateSessionEnvelope(envelope, options = {}) { ... }
function validateInitialTriggerEnvelope(triggerData) { ... }
function processTriggerFile(context, filePath) { ... }
function handleReviewGateToolCall(context, toolData, sessionContract) { ... }
function openReviewGatePopup(context, options = {}) { ... }
function writeSessionResponse(triggerId, inputText, attachments = [], eventType = 'MCP_RESPONSE', sessionContract = null) { ... }
function handleImageUpload(triggerId) { ... }

module.exports = {
  activate,
  deactivate
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a minimal deterministic test seam to the single-file extension</name>
  <files>V2/cursor-extension/extension.js</files>
  <read_first>V2/cursor-extension/extension.js, .planning/phases/03-scoped-ipc-security/03-03-SUMMARY.md, .planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md</read_first>
  <action>Add a minimal `__testHooks` export, or an equivalently narrow test-only surface under `module.exports`, so repo-local Node tests can drive the shipped extension without rewire libraries. Expose only the helpers actually needed by the harness: a full runtime resetter for `chatPanel`, `activeMcpSession`, `handledTriggerIds`, popup context, timers, and current recording; deterministic setters for fake panel or output-channel state where needed; and the runtime helpers used by the tests such as `processTriggerFile`, `openReviewGatePopup`, `handleImageUpload`, `writeSessionResponse`, `writeSessionResult`, `validateSessionEnvelope`, `validateInitialTriggerEnvelope`, and session-path helpers. Keep activation behavior, command registration, and packaging unchanged. Do not split `extension.js`, add Jest/Vitest, or introduce a new runtime abstraction just for tests.</action>
  <verify>node - <<'NODE'
const Module = require('module');
const originalLoad = Module._load;
Module._load = function(request, parent, isMain) {
  if (request === 'vscode') {
    return {
      window: {},
      commands: { registerCommand() { return { dispose() {} }; } },
      workspace: {},
      Uri: { file(fsPath) { return { fsPath }; } },
      ViewColumn: { Beside: 2 }
    };
  }
  return originalLoad.apply(this, arguments);
};

const extension = require('./V2/cursor-extension/extension.js');
const hooks = extension.__testHooks;
for (const key of ['resetState', 'processTriggerFile', 'handleImageUpload', 'writeSessionResponse', 'validateInitialTriggerEnvelope']) {
  if (!hooks || typeof hooks[key] !== 'function') {
    throw new Error(`missing ${key}`);
  }
}
console.log('ok');
NODE
  </verify>
  <acceptance_criteria>
    - The shipped extension can be loaded in tests under a stubbed `vscode` module.
    - The exposed test surface is intentionally narrow and resettable, not a second public API.
  </acceptance_criteria>
  <done>The single-file extension has a deterministic test seam that is sufficient for repo-local runtime regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add built-in Node tests for trigger proof intake, popup lifecycle, session routing, and attachment handling</name>
  <files>tests/node/load-extension.js, tests/node/extension.runtime.test.js</files>
  <read_first>V2/cursor-extension/extension.js, V2/cursor-extension/package.json, .planning/phases/02-session-routing-reliability/02-02-SUMMARY.md, .planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md</read_first>
  <action>Create `tests/node/load-extension.js` and `tests/node/extension.runtime.test.js` using only Node built-ins (`node:test`, `assert/strict`, `fs`, `path`, `os`, `module`). The loader must stub the `vscode` module, provide fake webview panels and output channels, and isolate runtime state with unique `REVIEW_GATE_USER_ID` values. The test suite must cover the shipped extension behavior: accepting a valid signed trigger and rejecting a forged or stale one before popup mutation; writing a `busy` result when a second trigger arrives while an MCP popup is active; disposing the popup and writing the documented cancelled result; and exercising attachment flow through `handleImageUpload` plus `writeSessionResponse` so uploaded image metadata lands in the session response file. Avoid browser automation, external packages, or refactoring the extension into new modules for elegance alone.</action>
  <verify>node --test tests/node/*.test.js</verify>
  <acceptance_criteria>
    - The Node suite covers popup lifecycle, session routing, and attachment handling for the shipped extension file.
    - The suite runs under the repo’s current Node runtime with no third-party test framework.
  </acceptance_criteria>
  <done>Maintainers can run automated extension regression checks for the behaviors required by QUAL-02.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `node --test tests/node/*.test.js`
- [ ] Confirm the test harness uses only built-in Node tooling and a stubbed `vscode` module, not real Cursor automation.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No heavyweight JS test framework or extension architecture rewrite was introduced
- Maintainers can run repo-local extension regression checks that prove the shipped popup/session flow still satisfies QUAL-02.
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-hardening-verification/04-02-SUMMARY.md`
</output>
