---
phase: 04-automated-hardening-verification
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - V2/install.sh
  - V2/install.ps1
  - V2/install.bat
  - tests/python/test_update_mcp_config.py
  - tests/smoke/test_installers.py
  - scripts/run_review_gate_regression_checks.py
autonomous: true
requirements:
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - "Maintainer can run non-destructive installer smoke checks against temporary home and install roots instead of the real Cursor profile."
    - "Maintainer can verify MCP configuration merge and remove behavior with automated fixtures instead of manual `mcp.json` edits."
    - "Maintainer can run one repo-root command that executes the Python server suite, the Node extension suite, and the installer smoke suite before release."
  artifacts:
    - path: V2/install.sh
      provides: "POSIX installer with a repo-local smoke-mode contract that redirects config and install roots without touching the real workstation."
      contains: "REVIEW_GATE_SMOKE"
    - path: V2/install.ps1
      provides: "PowerShell installer with the same smoke-mode contract used by the repo smoke harness."
      contains: "REVIEW_GATE_SMOKE"
    - path: V2/install.bat
      provides: "Batch installer with the same smoke-mode contract used by the repo smoke harness."
      contains: "REVIEW_GATE_SMOKE"
    - path: tests/python/test_update_mcp_config.py
      provides: "Automated fixture coverage for `update_mcp_config.py` merge, remove, and template-path injection behavior."
      contains: "merge"
    - path: tests/smoke/test_installers.py
      provides: "Installer smoke driver that runs the scripts in isolated temp roots and asserts copied assets plus merged config."
      contains: "REVIEW_GATE_SMOKE"
    - path: scripts/run_review_gate_regression_checks.py
      provides: "Single release-check entrypoint that runs all Phase 4 suites from the repo root."
      contains: "run_suite"
  key_links:
    - from: tests/python/test_update_mcp_config.py
      to: V2/update_mcp_config.py
      via: "fixture-driven merge/remove CLI coverage against the shipped `V2/mcp.json` template"
      pattern: "merge|remove|review-gate-v2|mcpServers"
    - from: tests/smoke/test_installers.py
      to: V2/install.sh
      via: "shared smoke-mode env contract for temp HOME, install dir, and skipped system side effects"
      pattern: "REVIEW_GATE_SMOKE|REVIEW_GATE_TEST_HOME|REVIEW_GATE_TEST_INSTALL_DIR"
    - from: tests/smoke/test_installers.py
      to: V2/install.ps1
      via: "shared smoke-mode env contract for temp HOME, install dir, and skipped system side effects"
      pattern: "REVIEW_GATE_SMOKE|REVIEW_GATE_TEST_HOME|REVIEW_GATE_TEST_INSTALL_DIR"
    - from: tests/smoke/test_installers.py
      to: V2/install.bat
      via: "shared smoke-mode env contract for temp HOME, install dir, and skipped system side effects"
      pattern: "REVIEW_GATE_SMOKE|REVIEW_GATE_TEST_HOME|REVIEW_GATE_TEST_INSTALL_DIR"
    - from: scripts/run_review_gate_regression_checks.py
      to: tests/python/test_review_gate_v2_mcp.py
      via: "repo-root subprocess suite execution"
      pattern: "test_review_gate_v2_mcp"
    - from: scripts/run_review_gate_regression_checks.py
      to: tests/node/extension.runtime.test.js
      via: "repo-root subprocess suite execution"
      pattern: "extension.runtime.test.js"
---

<objective>
Add installer smoke coverage and a single release-check entrypoint that runs the full Phase 4 regression set.

Purpose: Finish the phase by giving maintainers one repeatable, low-friction command that validates the shipped runtime surface before release, including installer/config behavior.
Output: Non-destructive installer smoke mode, config-helper fixture tests, and a repo-root runner for the Python, Node, and installer suites.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automated-hardening-verification/04-CONTEXT.md
@.planning/phases/01-installation-integrity/01-02-SUMMARY.md
@.planning/phases/01-installation-integrity/01-03-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@V2/install.sh
@V2/install.ps1
@V2/install.bat
@V2/update_mcp_config.py
@V2/mcp.json
@V2/cursor-extension/package.json

<interfaces>
From `V2/update_mcp_config.py`:
```python
def merge(
    *,
    config_path: Path,
    template_path: Path,
    server_name: str,
    install_dir: str,
    python_cmd: str,
) -> None: ...

def remove(*, config_path: Path, server_name: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared non-destructive smoke-mode contract to all supported installers</name>
  <files>V2/install.sh, V2/install.ps1, V2/install.bat</files>
  <read_first>V2/install.sh, V2/install.ps1, V2/install.bat, V2/update_mcp_config.py, .planning/phases/01-installation-integrity/01-02-SUMMARY.md, .planning/phases/01-installation-integrity/01-03-SUMMARY.md</read_first>
  <action>Add the same smoke-mode contract to all three installers using explicit environment variables: `REVIEW_GATE_SMOKE=1`, `REVIEW_GATE_TEST_HOME`, `REVIEW_GATE_TEST_INSTALL_DIR`, `REVIEW_GATE_SKIP_DEP_INSTALL=1`, `REVIEW_GATE_SKIP_EXTENSION_INSTALL=1`, and `REVIEW_GATE_SKIP_SERVER_SMOKE=1`. In smoke mode, each installer must redirect the Cursor config path and install root into those temp paths, skip package-manager and real Cursor-extension side effects, but still exercise the real file-copy and MCP-config mutation logic used by the shipped installer. Keep normal installer behavior unchanged when the smoke vars are unset. Do not add CI-only branches, and do not replace the current install flow with a different architecture; this task is only about making the existing scripts safely testable.</action>
  <verify>rg -n 'REVIEW_GATE_SMOKE|REVIEW_GATE_TEST_HOME|REVIEW_GATE_TEST_INSTALL_DIR|REVIEW_GATE_SKIP_DEP_INSTALL|REVIEW_GATE_SKIP_EXTENSION_INSTALL|REVIEW_GATE_SKIP_SERVER_SMOKE' V2/install.sh V2/install.ps1 V2/install.bat</verify>
  <acceptance_criteria>
    - All three installers understand the same smoke-mode variables.
    - Smoke mode redirects install and config paths away from the real workstation while preserving the shipped copy and merge logic.
  </acceptance_criteria>
  <done>The installers can be exercised automatically in temp roots without mutating the maintainer’s real Cursor profile.</done>
</task>

<task type="auto">
  <name>Task 2: Add config-helper fixtures and installer smoke tests</name>
  <files>tests/python/test_update_mcp_config.py, tests/smoke/test_installers.py</files>
  <read_first>V2/update_mcp_config.py, V2/mcp.json, V2/install.sh, V2/install.ps1, V2/install.bat, V2/cursor-extension/package.json</read_first>
  <action>Create `tests/python/test_update_mcp_config.py` with stdlib `unittest` coverage for the shipped helper: merge preserves unrelated `mcpServers`, install-dir and Python-path injection work for both POSIX and Windows-style paths, remove deletes only `review-gate-v2`, invalid JSON fails cleanly, and a missing template server name fails clearly. Create `tests/smoke/test_installers.py` with stdlib `unittest`, `tempfile`, and `subprocess` to run installer smoke mode in isolated temp directories. The smoke suite must always run `install.sh` when the current shell can execute it, and it must run `install.ps1` or `install.bat` when `pwsh`, `powershell.exe`, or `cmd.exe` are available; otherwise skip those subtests explicitly with a clear reason. Assert copied runtime assets, backup-file behavior, and merged `mcp.json` contents. Do not shell out to package managers or real Cursor CLI installs in the smoke suite.</action>
  <verify>python3 -m unittest tests/python/test_update_mcp_config.py tests/smoke/test_installers.py -v</verify>
  <acceptance_criteria>
    - MCP config merge/remove behavior is covered by automated fixtures.
    - Installer smoke tests use the new smoke-mode contract and validate copied assets plus merged config in temp roots.
  </acceptance_criteria>
  <done>Maintainers have automated smoke checks for the shared config helper and the shipped installer scripts, satisfying the core of QUAL-03.</done>
</task>

<task type="auto">
  <name>Task 3: Add one repo-root release-check command that runs the full Phase 4 regression set</name>
  <files>scripts/run_review_gate_regression_checks.py</files>
  <read_first>tests/python/test_review_gate_v2_mcp.py, tests/node/extension.runtime.test.js, tests/python/test_update_mcp_config.py, tests/smoke/test_installers.py</read_first>
  <action>Create `scripts/run_review_gate_regression_checks.py` as the single repo-root entrypoint for maintainers before release. Use only Python stdlib `subprocess` and `argparse`. It must run the Python server suite from `04-01`, the Node extension suite from `04-02`, and the config plus installer smoke suite from this plan in a clear fixed order, fail fast on the first non-zero exit by default, and support a lightweight `--list` or `--suite` option so maintainers can run subsets while debugging. Keep default runtime short and output readable. Do not add tox, npm workspaces, GitHub Actions, or marketplace/release automation in this phase.</action>
  <verify>python3 scripts/run_review_gate_regression_checks.py</verify>
  <acceptance_criteria>
    - Maintainers can run one repo-root command to execute all Phase 4 regression checks before release.
    - The runner returns non-zero on failure and supports targeted suite reruns.
  </acceptance_criteria>
  <done>The phase has one concrete release-check command that exercises Python server, extension, and installer/config regressions together.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m unittest tests/python/test_update_mcp_config.py tests/smoke/test_installers.py -v`
- [ ] `python3 scripts/run_review_gate_regression_checks.py --list`
- [ ] `python3 scripts/run_review_gate_regression_checks.py`
- [ ] Confirm the runner executes the suites from `04-01`, `04-02`, and this plan rather than creating a second disconnected verification path.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new heavy framework or CI-only dependency was introduced
- Maintainers can run a single repo-root release-check command that includes installer/config smoke coverage and completes QUAL-03.
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-hardening-verification/04-03-SUMMARY.md`
</output>
