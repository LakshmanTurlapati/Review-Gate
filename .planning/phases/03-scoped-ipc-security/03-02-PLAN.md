---
phase: 03-scoped-ipc-security
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - V2/review_gate_v2_mcp.py
  - V2/cursor-extension/extension.js
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - "Review Gate rejects malformed or unauthenticated trigger, acknowledgement, response, and speech envelopes before they can change the active session state."
    - "Partial writes, symlink tricks, or unexpected file locations do not get treated as trusted IPC messages."
    - "The status surface used by the extension no longer exposes raw prompt text, user feedback, attachment metadata, or speech transcripts."
  artifacts:
    - path: V2/review_gate_v2_mcp.py
      provides: "Protocol-versioned session-envelope validation, atomic JSON writes, and redacted status heartbeat output for the Python server."
      contains: "_validate_session_envelope"
    - path: V2/cursor-extension/extension.js
      provides: "Matching envelope validation, atomic JSON writes, and redacted status-file monitoring in the Cursor extension."
      contains: "validateSessionEnvelope"
  key_links:
    - from: V2/review_gate_v2_mcp.py
      to: V2/cursor-extension/extension.js
      via: "shared `protocol_version` and `session_token` fields on trigger, ack, response, and speech envelopes, with new trigger acceptance gated by required protocol fields plus same-root runtime-path validation before the popup state is touched"
      pattern: "protocol_version|session_token"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "atomic JSON writes and same-root path validation for active IPC files"
      pattern: "writeJsonAtomically|_write_json_atomically"
    - from: V2/review_gate_v2_mcp.py
      to: V2/cursor-extension/extension.js
      via: "redacted heartbeat/status JSON replaces raw temp-log inspection for MCP liveness"
      pattern: "review_gate_status\\.json"
---

<objective>
Require authenticated session envelopes and reduce temp-backed logging exposure on top of the scoped session storage from Plan 03-01.

Purpose: Make untrusted local messages fail closed and remove raw sensitive payloads from the runtime status surface.
Output: Updated Python and extension runtimes that validate protocol fields and session tokens before acting, write envelope JSON atomically, and use a redacted status heartbeat instead of raw temp logs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoped-ipc-security/03-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@V2/review_gate_v2_mcp.py
@V2/cursor-extension/extension.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protocol-versioned session authentication and strict envelope validation in both runtimes</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js, .planning/phases/03-scoped-ipc-security/03-01-PLAN.md</read_first>
  <action>Build directly on the Plan 03-01 session helpers. In the Python server, generate a per-session `session_token` plus a constant `protocol_version` for every active trigger in `_trigger_cursor_popup_immediately`; include both fields in the trigger envelope, store the token with the active session, and require the same fields on the matching acknowledgement, response, speech trigger, and speech response envelopes. In the extension, treat a new trigger as valid only when it arrives as a same-root regular file under the Review Gate runtime root and contains the expected `trigger_id`, `session_token`, `protocol_version`, `system`, `editor`, and `source` contract; after activation, track `activeMcpSession.sessionToken` and reject any follow-up envelope whose session fields do not match the active contract. Add explicit validation helpers named `_validate_session_envelope(...)` in Python and `validateSessionEnvelope(...)` in JavaScript, and make them reject malformed or mismatched envelopes before popup state, response state, or speech state changes. Keep the current temp-file transport and single-popup UX; do not introduce a daemon or external key store in this phase.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && rg -n 'protocol_version|session_token|_validate_session_envelope|validateSessionEnvelope' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - Trigger, acknowledgement, response, and speech envelopes all carry and validate the same `protocol_version` and `session_token`.
    - Invalid or mismatched local envelopes are deleted or ignored before they can mutate the active popup or return path.
  </acceptance_criteria>
  <done>Review Gate now treats Phase 2 session files as authenticated session envelopes rather than trusting any JSON file with a matching filename.</done>
</task>

<task type="auto">
  <name>Task 2: Make all active IPC JSON writes atomic and path-safe</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</read_first>
  <action>Add `_write_json_atomically(path, payload)` in Python and `writeJsonAtomically(filePath, payload)` in JavaScript. Use those helpers for every trigger, acknowledgement, response, speech trigger, speech response, and status JSON write. Each helper must write to a temp file inside the same session directory, fsync where the runtime allows, then rename into place so the other runtime never reads a partially written envelope. On the read path, reject symlinks, non-regular files, and resolved paths that escape the Review Gate runtime root before parsing JSON. Preserve current timeout behavior and response semantics while tightening the file trust boundary.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && rg -n '_write_json_atomically|writeJsonAtomically|lstat|resolve\\(' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - No active IPC JSON write path uses a bare direct write as the final step.
    - Read paths reject files that do not resolve to regular files under the Review Gate runtime root.
  </acceptance_criteria>
  <done>Review Gate no longer trusts partially written or path-manipulated session envelopes as valid IPC state.</done>
</task>

<task type="auto">
  <name>Task 3: Replace raw temp-log status monitoring with a redacted heartbeat surface and stop logging sensitive payload contents</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js, .planning/codebase/CONCERNS.md</read_first>
  <action>Remove the extension's dependence on `review_gate_v2.log` and any remaining shared temp log that carries payload content. In the Python server, write a bounded `review_gate_status.json` heartbeat file under the Review Gate runtime root containing only machine-readable status metadata such as timestamp, pid, server state, and last heartbeat age. Change `checkMcpStatus()` in the extension to read that status file instead of checking the mtime of a raw log. At the same time, redact or delete raw payload logging on both sides: stop logging full tool arguments, prompt bodies, user responses, attachment lists, base64 blobs, file contents, and speech transcripts to temp-backed files or console lines intended for IPC diagnostics. Keep stderr/output channel diagnostics, but only log trigger ids, status transitions, counts, and file basenames.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && rg -n 'review_gate_status\\.json' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js && ! rg -n 'review_gate_v2\\.log|REVIEW GATE USER INPUT:|Tool arguments:|Speech transcribed:|CREATING trigger file with data' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - Extension liveness no longer depends on a temp log file whose contents include runtime text.
    - Active runtime logs no longer emit raw prompts, user feedback, attachment metadata, or transcription text to temp-backed surfaces.
  </acceptance_criteria>
  <done>The IPC status surface is now redacted and bounded, and malformed or unauthenticated envelopes are rejected without exposing sensitive payloads in temp logs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile V2/review_gate_v2_mcp.py`
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `rg -n 'protocol_version|session_token|_validate_session_envelope|validateSessionEnvelope|review_gate_status\\.json' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js`
- [ ] `! rg -n 'review_gate_v2\\.log|REVIEW GATE USER INPUT:|Tool arguments:|Speech transcribed:|CREATING trigger file with data' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js`
- [ ] Confirm malformed or mismatched envelopes are ignored or deleted before they can update `activeMcpSession`, acknowledgement state, response state, or speech state.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Review Gate now validates active IPC envelopes by protocol and session token, writes them atomically, and removes raw temp-log exposure from the runtime status path, satisfying SEC-02 and the remaining logging portion of SEC-01.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoped-ipc-security/03-02-SUMMARY.md`
</output>
