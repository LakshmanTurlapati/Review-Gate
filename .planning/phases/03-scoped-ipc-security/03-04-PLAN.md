---
phase: 03-scoped-ipc-security
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - V2/review_gate_v2_mcp.py
  - V2/cursor-extension/extension.js
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - "Prompt text, attachment metadata, and speech content no longer escape to stderr or the Cursor output channel outside the session-directory lifecycle."
    - "The extension refuses a new MCP trigger unless the trigger envelope carries a valid HMAC proof derived from the active Review Gate runtime secret and current server heartbeat state before popup state is mutated."
    - "Busy, cancelled, timeout, and successful response handling still work after the first-trigger proof is enforced."
  artifacts:
    - path: V2/review_gate_v2_mcp.py
      provides: "Redacted diagnostic helpers plus runtime-secret-backed initial-trigger proof material written with each trigger envelope."
      contains: "_trigger_cursor_popup_immediately"
    - path: V2/cursor-extension/extension.js
      provides: "Extension-side runtime-secret and heartbeat-backed initial-trigger validation before `activeMcpSession` or popup state is set."
      contains: "processTriggerFile"
  key_links:
    - from: V2/review_gate_v2_mcp.py
      to: V2/cursor-extension/extension.js
      via: "initial trigger envelopes carry `trigger_signature` and `trigger_issued_at` derived from the shared runtime secret, and the extension rejects them before `handleReviewGateToolCall(...)` when the proof, freshness window, or heartbeat pid is wrong"
      pattern: "trigger_signature|trigger_issued_at|validateInitialTrigger"
    - from: V2/review_gate_v2_mcp.py
      to: V2/cursor-extension/extension.js
      via: "both runtimes apply the same redaction policy so diagnostics expose only trigger ids, statuses, counts, and safe basenames"
      pattern: "logUserInput|logger\\.info|logger\\.warning|logMessage"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "extension acknowledgement and response flow stays compatible with the authenticated session envelope contract from 03-02 after the initial-trigger proof gate is added"
      pattern: "sendExtensionAcknowledgement|writeSessionResponse|_wait_for_extension_acknowledgement"
---

<objective>
Close the remaining Phase 3 verification blockers without reopening the broader IPC-security scope.

Purpose: Finish SEC-01 and SEC-02 by eliminating residual diagnostic leakage and by authenticating the first trigger before the popup accepts a new MCP-owned session.
Output: One follow-up hardening pass in the Python server and Cursor extension that redacts remaining diagnostics and requires a server-backed trigger proof on initial trigger intake.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoped-ipc-security/03-CONTEXT.md
@.planning/phases/03-scoped-ipc-security/03-VERIFICATION.md
@.planning/phases/03-scoped-ipc-security/03-02-SUMMARY.md
@.planning/phases/03-scoped-ipc-security/03-03-SUMMARY.md
@V2/review_gate_v2_mcp.py
@V2/cursor-extension/extension.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the remaining non-session diagnostic leakage from the server and extension</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>.planning/phases/03-scoped-ipc-security/03-VERIFICATION.md, V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</read_first>
  <action>Use the verification findings as the source of truth. In `V2/cursor-extension/extension.js`, change `logUserInput(...)`, the webview message handlers around `logPastedImage`, `logDragDropImage`, `logImageRemoved`, file/image picker events, and any remaining review-text logging so attachment filenames, MIME types, image ids, selected file lists, prompt bodies, and user response text are never emitted to the output channel. Keep only trigger ids, event types, counts, and safe basenames where operationally necessary. In `V2/review_gate_v2_mcp.py`, scrub the remaining stderr logs in the chat, quick-review, file-review, and attachment return paths so prompt bodies, review instructions, attachment names, and response text are not logged; log only trigger ids, tool names, status transitions, counts, and safe basenames. Do not remove the status heartbeat or the existing busy/cancel/timeout diagnostics; narrow them to redacted metadata only.</action>
  <verify>
    <automated>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && ! rg -n 'Image pasted from clipboard:|Image dropped from drag and drop:|Image removed:|Files selected for review:|TEXT FEEDBACK:|SHUTDOWN CONFIRMED:|SHUTDOWN ALTERNATIVE:' V2/cursor-extension/extension.js && ! rg -n 'ACTIVATING Quick Review IMMEDIATELY for Cursor Agent:|ACTIVATING File Review IMMEDIATELY for Cursor Agent:|Added image to response:' V2/review_gate_v2_mcp.py</automated>
  </verify>
  <done>Prompt text and attachment metadata no longer leave the Review Gate session lifecycle through extension output-channel lines or Python stderr diagnostics, closing the remaining SEC-01 leak path.</done>
</task>

<task type="auto">
  <name>Task 2: Require live server-backed proof before the extension accepts a new MCP trigger</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>.planning/phases/03-scoped-ipc-security/03-VERIFICATION.md, .planning/phases/03-scoped-ipc-security/03-02-SUMMARY.md, V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</read_first>
  <action>Keep the temp-file transport and the Phase 2/03-02 session contract, but harden the first trigger intake with one concrete proof flow. In `V2/review_gate_v2_mcp.py`, add a runtime-secret helper under the Review Gate-owned runtime root, such as `review_gate_runtime_secret`, that lazily creates a random secret with restrictive permissions and reuses it for the active server lifetime. Update `_trigger_cursor_popup_immediately(...)` to include `trigger_issued_at` plus `trigger_signature`, where the signature is an HMAC-SHA256 over the immutable initial-trigger fields (`trigger_id`, `protocol_version`, `session_token`, `timestamp`, `pid`, and tool name) using that runtime secret. In `V2/cursor-extension/extension.js`, add `loadRuntimeSecret()` plus `validateInitialTriggerEnvelope(...)` that recomputes the HMAC, rejects signatures that do not match, rejects stale `trigger_issued_at` values outside a short freshness window, requires the trigger `pid` to match the live `review_gate_status.json` heartbeat pid/server_state, and still enforces the expected session path. Only after those checks pass may `processTriggerFile(...)` call `handleReviewGateToolCall(...)` or assign `activeMcpSession`. Rejected triggers must be logged in redacted form, removed from disk, and must not mutate popup state. Preserve the existing busy acknowledgement, cancellation, timeout, and response semantics after the proof gate passes.</action>
  <verify>
    <automated>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && rg -n 'trigger_signature|trigger_proof|runtime_secret|validateInitialTriggerEnvelope|verifyInitialTrigger' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js && rg -n 'processTriggerFile' V2/cursor-extension/extension.js</automated>
  </verify>
  <done>The extension now authenticates the initial trigger against live server-issued proof before opening a popup or claiming `activeMcpSession`, closing the remaining SEC-02 trigger-intake gap.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile V2/review_gate_v2_mcp.py`
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `! rg -n 'Image pasted from clipboard:|Image dropped from drag and drop:|Image removed:|Files selected for review:|TEXT FEEDBACK:|SHUTDOWN CONFIRMED:|SHUTDOWN ALTERNATIVE:' V2/cursor-extension/extension.js`
- [ ] `! rg -n 'ACTIVATING Quick Review IMMEDIATELY for Cursor Agent:|ACTIVATING File Review IMMEDIATELY for Cursor Agent:|Added image to response:' V2/review_gate_v2_mcp.py`
- [ ] `rg -n 'trigger_signature|trigger_proof|runtime_secret|validateInitialTriggerEnvelope|verifyInitialTrigger' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js`
- [ ] Confirm `processTriggerFile(...)` rejects a forged or stale trigger before `handleReviewGateToolCall(...)` runs or `activeMcpSession` is assigned.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 no longer leaks prompt or attachment data through diagnostics, and the extension authenticates the first trigger before it mutates MCP popup state, allowing SEC-01 and SEC-02 to pass re-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoped-ipc-security/03-04-SUMMARY.md`
</output>
