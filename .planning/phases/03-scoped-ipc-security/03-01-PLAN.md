---
phase: 03-scoped-ipc-security
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - V2/review_gate_v2_mcp.py
  - V2/cursor-extension/extension.js
autonomous: true
requirements:
  - SEC-01
user_setup: []
must_haves:
  truths:
    - "User feedback, attachment metadata, speech requests, and speech responses no longer live in flat shared temp filenames; they live under one Review Gate-owned runtime subtree keyed by session."
    - "Completing, cancelling, or timing out a Review Gate session removes its trigger, acknowledgement, response, speech, and audio artifacts instead of leaving sensitive data behind."
    - "Stale abandoned session directories are swept as a unit before they can be mistaken for a live exchange."
  artifacts:
    - path: V2/review_gate_v2_mcp.py
      provides: "Per-user runtime-root and per-session directory helpers plus session-directory cleanup for the Python MCP server."
      contains: "get_runtime_root"
    - path: V2/cursor-extension/extension.js
      provides: "Matching per-user runtime-root and per-session directory helpers plus extension-side session-directory cleanup."
      contains: "getRuntimeRoot"
  key_links:
    - from: V2/review_gate_v2_mcp.py
      to: V2/cursor-extension/extension.js
      via: "shared runtime root `review-gate-v2/<user>/sessions/<trigger_id>/...` derived in both runtimes"
      pattern: "sessions[/\\\\].+review_gate_(trigger|ack|response|speech_)"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "session-owned speech trigger, speech response, and audio cleanup bound to the same session directory"
      pattern: "speechTrigger|speechResponse|review_gate_audio_"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "terminal session cleanup removes the entire session subtree after submit, cancel, timeout, or stale sweep"
      pattern: "cleanupSessionDirectory|_cleanup_session_directory"
---

<objective>
Move active Review Gate IPC and transient session data into one Review Gate-owned runtime subtree with deterministic cleanup.

Purpose: Reduce broad temp-directory exposure before tightening envelope trust rules on top of the Phase 2 transport.
Output: Updated Python and extension runtimes that derive all active session artifacts from a shared per-user runtime root and remove those artifacts after handoff.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoped-ipc-security/03-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/02-session-routing-reliability/02-01-SUMMARY.md
@.planning/phases/02-session-routing-reliability/02-02-SUMMARY.md
@V2/review_gate_v2_mcp.py
@V2/cursor-extension/extension.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one per-user Review Gate runtime root and per-session directory contract in both runtimes</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js, .planning/phases/03-scoped-ipc-security/03-CONTEXT.md</read_first>
  <action>Add matching runtime path helpers in both files: Python `get_runtime_root()`, `_session_dir(trigger_id)`, and the existing `_session_file(kind, trigger_id)` layered on top; JavaScript `getRuntimeRoot()`, `getSessionDir(triggerId)`, and the existing `getSessionFilePath(kind, triggerId)` layered on top. The runtime root must live under the OS temp directory in the exact subtree `review-gate-v2/<sanitized-user-id>/`, with session data under `sessions/<trigger_id>/`. Preserve the Phase 2 file naming convention (`review_gate_trigger_<trigger_id>.json`, `review_gate_ack_<trigger_id>.json`, `review_gate_response_<trigger_id>.json`, `review_gate_speech_trigger_<trigger_id>.json`, `review_gate_speech_response_<trigger_id>.json`) but place those files inside the session directory instead of the temp root. Ensure the runtime root and session directories are created lazily, and use restrictive POSIX permissions where the runtime allows. Do not replace temp-file IPC with sockets, named pipes, or a daemon in this plan.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && rg -n 'get_runtime_root|_session_dir|getRuntimeRoot|getSessionDir' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - Both runtimes derive active trigger, acknowledgement, response, and speech paths from one shared per-user runtime root under `review-gate-v2/<user>/sessions/<trigger_id>/`.
    - The Phase 2 session filename pattern remains recognizable, but no active-flow session files are written directly into the flat temp root.
  </acceptance_criteria>
  <done>Phase 2 helper-based routing now points at Review Gate-owned per-session directories instead of broad shared temp filenames.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the Python MCP server around session directories and whole-session cleanup</name>
  <files>V2/review_gate_v2_mcp.py</files>
  <read_first>V2/review_gate_v2_mcp.py, .planning/codebase/INTEGRATIONS.md, .planning/phases/02-session-routing-reliability/02-02-SUMMARY.md</read_first>
  <action>Update `_trigger_cursor_popup_immediately`, `_wait_for_extension_acknowledgement`, `_wait_for_user_input`, `_cleanup_stale_session_files`, `_process_speech_request`, `_write_speech_response`, and `_monitor_shutdown` so the Python server reads and writes only within `_session_dir(trigger_id)`. Add `_cleanup_session_directory(trigger_id, keep_status = False)` or equivalent and call it after every terminal outcome (`response`, `busy`, `cancelled`, `timeout`, or `error`) plus stale cleanup passes. Keep the current tool name `review_gate_chat`, the current response timeout values, and the Phase 2 busy/cancel recovery semantics. Do not introduce a new queue or multi-session UX.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && rg -n 'get_runtime_root|_session_dir|_cleanup_session_directory' V2/review_gate_v2_mcp.py && ! rg -n 'Path\\(get_temp_path\\(\"review_gate_(trigger|ack|response|speech_response|speech_trigger)' V2/review_gate_v2_mcp.py</verify>
  <acceptance_criteria>
    - The Python server no longer writes active trigger, acknowledgement, response, or speech files directly into `/tmp` or the bare system temp directory.
    - Finishing or abandoning a session removes its entire session subtree rather than unlinking only one or two files.
  </acceptance_criteria>
  <done>The MCP server scopes each active exchange to one session directory and clears that directory when the exchange is over.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor the extension to keep attachments, speech artifacts, and response files inside the owning session directory</name>
  <files>V2/cursor-extension/extension.js</files>
  <read_first>V2/cursor-extension/extension.js, .planning/codebase/CONCERNS.md, .planning/phases/02-session-routing-reliability/02-02-SUMMARY.md</read_first>
  <action>Update `removeSessionFile`, `removeSessionAudioFiles`, `cleanupSessionSpeechArtifacts`, `cleanupStaleSessionFiles`, `processTriggerFile`, `sendExtensionAcknowledgement`, `writeSessionResult`, `writeSessionResponse`, `handleSpeechToText`, `startNodeRecording`, and `stopNodeRecording` so every active IPC JSON file and every `review_gate_audio_<trigger_id>_*.wav` file lives inside `getSessionDir(triggerId)`. Remove the broad temp audit file `review_gate_user_inputs.log`; if operational logging is still needed, keep it in the Cursor output channel only or in short-lived redacted session-local metadata that is deleted with the session directory. Preserve manual popup support by continuing to synthesize a manual trigger id when no MCP trigger owns the popup.</action>
  <verify>node --check V2/cursor-extension/extension.js && rg -n 'getRuntimeRoot|getSessionDir|cleanupSessionDirectory' V2/cursor-extension/extension.js && ! rg -n 'review_gate_user_inputs\\.log' V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - The extension writes session replies, speech requests, speech results, and audio captures into the owning session directory only.
    - Raw user feedback is no longer appended to a shared `review_gate_user_inputs.log` file in the temp root.
  </acceptance_criteria>
  <done>The extension now keeps active response and speech artifacts inside the session subtree and deletes them with the rest of the session-owned data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile V2/review_gate_v2_mcp.py`
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `rg -n 'get_runtime_root|_session_dir|getRuntimeRoot|getSessionDir' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js`
- [ ] `! rg -n 'review_gate_user_inputs\\.log' V2/cursor-extension/extension.js`
- [ ] Confirm the active trigger, acknowledgement, response, speech, and audio artifacts now live under `review-gate-v2/<user>/sessions/<trigger_id>/`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Review Gate now stores active IPC artifacts only inside session-owned runtime directories and removes them after the session ends, satisfying the storage and cleanup portion of SEC-01.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoped-ipc-security/03-01-SUMMARY.md`
</output>
