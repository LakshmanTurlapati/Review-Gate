---
phase: 02-session-routing-reliability
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - V2/review_gate_v2_mcp.py
  - V2/cursor-extension/extension.js
autonomous: true
requirements:
  - SESS-01
  - SESS-03
user_setup: []
must_haves:
  truths:
    - "If another MCP request arrives while the popup already belongs to an active session, the new request gets an explicit busy or cancelled outcome instead of hijacking the current exchange."
    - "Timed-out, failed, or abandoned popup handoffs return clear status text to Cursor instead of stalling silently."
    - "Speech recording and transcription results are applied only to the active session that started them, and stale temp files are cleaned before they can be reused."
  artifacts:
    - path: V2/review_gate_v2_mcp.py
      provides: "Structured session outcome parsing plus stale-session cleanup for popup and speech artifacts."
      contains: "_cleanup_stale_session_files"
    - path: V2/cursor-extension/extension.js
      provides: "Explicit busy, cancelled, and recovery result writing for the single active popup session."
      contains: "writeSessionResult"
  key_links:
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "ack and response envelopes carrying `status` and `event_type` for busy or cancelled sessions"
      pattern: "SESSION_BUSY|SESSION_CANCELLED|status"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "stale cleanup of session-owned trigger, ack, response, and speech files before reuse"
      pattern: "cleanup.*session"
    - from: V2/cursor-extension/extension.js
      to: V2/review_gate_v2_mcp.py
      via: "speech trigger and response files bound to `activeMcpSession.triggerId`"
      pattern: "review_gate_speech_"
---

<objective>
Add deterministic recovery behavior so overlap, timeout, or abandonment produces an explicit session outcome instead of corrupting or stalling the popup exchange.

Purpose: Finish Phase 2 by turning the session-scoped file contract into a complete recovery path for busy, timed-out, cancelled, and stale flows.
Output: Updated Python and extension runtimes that reject overlapping sessions cleanly, clean stale temp files, and keep speech results bound to the owning popup session.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-routing-reliability/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@V2/review_gate_v2_mcp.py
@V2/cursor-extension/extension.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit explicit busy and cancelled outcomes from the extension instead of overwriting the active popup session</name>
  <files>V2/cursor-extension/extension.js</files>
  <read_first>V2/cursor-extension/extension.js, .planning/phases/02-session-routing-reliability/02-01-PLAN.md</read_first>
  <action>Add `writeSessionResult(triggerId, status, payload = {})` and `cleanupStaleSessionFiles(maxAgeMs)` helpers. When a new MCP trigger arrives while `activeMcpSession` is already awaiting user input, do not overwrite the panel or session owner; instead write `review_gate_ack_<trigger_id>.json` with `acknowledged: false` and `status: "busy"`, plus a matching `review_gate_response_<trigger_id>.json` with `status: "busy"` and `event_type: "SESSION_BUSY"` that identifies the active owner. On popup disposal for an MCP-owned session, write a matching `status: "cancelled"` and `event_type: "SESSION_CANCELLED"` result before clearing `activeMcpSession`. Keep manual popup opens unaffected.</action>
  <verify>node --check V2/cursor-extension/extension.js && rg -n 'writeSessionResult|cleanupStaleSessionFiles|SESSION_BUSY|SESSION_CANCELLED|status' V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - Overlapping MCP triggers no longer replace the active popup owner in-place.
    - Closing an MCP-owned popup writes an explicit cancelled session result instead of leaving the server to stall.
  </acceptance_criteria>
  <done>The extension now turns overlap and abandonment into explicit session outcomes instead of silent state corruption.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the Python server to return recovery outcomes and clean stale session files</name>
  <files>V2/review_gate_v2_mcp.py</files>
  <read_first>V2/review_gate_v2_mcp.py, .planning/phases/02-session-routing-reliability/02-01-PLAN.md</read_first>
  <action>Add `_cleanup_stale_session_files(max_age_seconds=600)` and call it before creating a new trigger plus after each completed or failed session. Change `_wait_for_extension_acknowledgement` and `_wait_for_user_input` to parse structured `status` and `event_type` fields from acknowledgement and response JSON, stop waiting immediately on `busy`, `cancelled`, or explicit `error` outcomes, and return clear `BUSY:`, `TIMEOUT:`, or `ERROR:` text back through `review_gate_chat` rather than continuing to stall. Keep the existing `review_gate_chat` tool name and temp-file transport. Do not add auth tokens, file-permission hardening, or other Phase 3 security work in this plan.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && rg -n '_cleanup_stale_session_files|SESSION_BUSY|SESSION_CANCELLED|BUSY:|TIMEOUT:|acknowledged' V2/review_gate_v2_mcp.py</verify>
  <acceptance_criteria>
    - The server no longer treats failed acknowledgement, busy sessions, or cancelled popups as a reason to keep waiting for five minutes.
    - Session-owned temp files older than the cleanup window are deleted before they can be mistaken for live traffic.
  </acceptance_criteria>
  <done>Cursor receives explicit recovery text for busy, timeout, and cancelled popup flows, and stale session files are cleaned proactively.</done>
</task>

<task type="auto">
  <name>Task 3: Bind speech and cleanup logic to the active session owner</name>
  <files>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</files>
  <read_first>V2/review_gate_v2_mcp.py, V2/cursor-extension/extension.js</read_first>
  <action>Update `startNodeRecording`, `stopNodeRecording`, `handleSpeechToText`, `_process_speech_request`, `_write_speech_response`, and extension disposal or timeout cleanup so speech trigger and response files are ignored unless their `trigger_id` matches `activeMcpSession.triggerId`. Delete orphaned speech trigger, speech response, and audio files older than the same cleanup window, and ensure an abandoned recording cannot inject transcription into a later popup session. Preserve the current SoX plus Faster-Whisper pipeline and the single-popup UX.</action>
  <verify>python3 -m py_compile V2/review_gate_v2_mcp.py && node --check V2/cursor-extension/extension.js && rg -n 'review_gate_speech_|activeMcpSession\\.triggerId|cleanupStaleSessionFiles|_cleanup_stale_session_files' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - Speech results are ignored when they belong to a session that is no longer active.
    - Old speech temp artifacts are cleaned instead of remaining available for later stale reads.
  </acceptance_criteria>
  <done>Speech side-channel behavior is now bound to the same active-session lifecycle as the popup and cannot leak across stale sessions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m py_compile V2/review_gate_v2_mcp.py`
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `rg -n 'SESSION_BUSY|SESSION_CANCELLED|writeSessionResult|cleanupStaleSessionFiles|_cleanup_stale_session_files' V2/review_gate_v2_mcp.py V2/cursor-extension/extension.js`
- [ ] Confirm the server stops waiting immediately for busy or cancelled outcomes instead of falling through to the full response timeout.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Overlapping, timed-out, and abandoned popup sessions now return explicit recovery outcomes and clean their stale temp files, satisfying the recovery guarantees required for SESS-03 without changing the current single-popup UX.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-routing-reliability/02-02-SUMMARY.md`
</output>
