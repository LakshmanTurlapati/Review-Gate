---
phase: 03-scoped-ipc-security
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - V2/cursor-extension/extension.js
autonomous: true
requirements:
  - SEC-03
user_setup: []
must_haves:
  truths:
    - "The popup no longer depends on a CDN or any other external asset host to render its controls."
    - "Dynamic popup content is inserted through DOM-safe APIs instead of `innerHTML`-based string assembly."
    - "The webview enforces a strict Content Security Policy that still preserves the current image, drag-drop, and speech interactions."
  artifacts:
    - path: V2/cursor-extension/extension.js
      provides: "Webview HTML generation with a strict CSP, nonce-based script tag, and local icon/rendering helpers."
      contains: "Content-Security-Policy"
  key_links:
    - from: V2/cursor-extension/extension.js
      to: "Cursor webview runtime"
      via: "nonce-based inline script plus `webview.cspSource` CSP for the popup HTML"
      pattern: "Content-Security-Policy|nonce-"
    - from: V2/cursor-extension/extension.js
      to: "popup image preview rendering"
      via: "DOM node construction with `createElement`, `textContent`, and explicit event listeners"
      pattern: "createElement|textContent"
    - from: V2/cursor-extension/extension.js
      to: "popup icon rendering"
      via: "inline SVG or extension-owned data URI assets instead of Font Awesome CDN"
      pattern: "svg|data:image"
---

<objective>
Harden the Review Gate popup so it renders with local assets only, enforces a strict CSP, and removes unsafe HTML assembly patterns.

Purpose: Finish Phase 3 by closing the remaining webview trust gaps without changing the visible product surface more than necessary.
Output: Updated popup HTML generation in `extension.js` that uses local or inline assets, a nonce-based CSP, and DOM-safe rendering for previews and actions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scoped-ipc-security/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@V2/cursor-extension/extension.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a strict webview CSP and remove external asset loading from the popup HTML</name>
  <files>V2/cursor-extension/extension.js</files>
  <read_first>V2/cursor-extension/extension.js, .planning/codebase/CONCERNS.md</read_first>
  <action>Refactor `openReviewGatePopup()` and `getReviewGateHTML()` so the popup HTML is generated with an explicit CSP based on `chatPanel.webview.cspSource` and a generated script nonce. Replace the Font Awesome CDN stylesheet with extension-owned assets only: use inline SVG icon helpers or extension-owned data URIs for the mic, image, send, close, and drag-drop affordances rather than any external stylesheet or script host. Keep the current single-file extension structure if that is the lowest-risk path, but require a CSP equivalent to `default-src 'none'; img-src ${webview.cspSource} data:; style-src ${webview.cspSource} 'unsafe-inline'; script-src 'nonce-<nonce>'`. Preserve the current image preview and speech interaction UX.</action>
  <verify>node --check V2/cursor-extension/extension.js && rg -n 'Content-Security-Policy|cspSource|nonce-' V2/cursor-extension/extension.js && ! rg -n 'cdnjs|font-awesome|https://cdnjs' V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - The popup HTML contains a strict CSP and no CDN stylesheet reference.
    - All icons used by the popup render from extension-owned inline or local assets only.
  </acceptance_criteria>
  <done>The webview no longer depends on external asset hosts and enforces a strict CSP compatible with the popup's current behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace unsafe HTML assembly with DOM-safe rendering helpers</name>
  <files>V2/cursor-extension/extension.js</files>
  <read_first>V2/cursor-extension/extension.js</read_first>
  <action>Remove `innerHTML`-based dynamic rendering from the popup script, especially the image preview and inline button markup in `handleImageUploaded()`. Build those DOM nodes with `document.createElement`, `textContent`, `setAttribute`, and explicit event listeners instead of string interpolation and inline `onclick`. Also stop embedding raw dynamic strings directly into the initial HTML template where they can break markup: serialize booleans and strings through JSON-safe literals or dedicated escaping helpers before inserting `title`, placeholder text, or prompt text into the page. Keep the existing drag/drop, paste, image removal, message sending, and speech UI flows functionally unchanged.</action>
  <verify>node --check V2/cursor-extension/extension.js && ! rg -n 'innerHTML\\s*=|onclick=' V2/cursor-extension/extension.js && rg -n 'createElement|textContent|addEventListener' V2/cursor-extension/extension.js</verify>
  <acceptance_criteria>
    - No dynamic popup rendering path relies on `innerHTML` assignment or inline event-handler attributes.
    - User-controlled strings reach the DOM through text-safe APIs or explicit escaping helpers instead of raw HTML interpolation.
  </acceptance_criteria>
  <done>The popup now renders dynamic content through DOM-safe APIs, closing the remaining HTML-injection path required by SEC-03.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check V2/cursor-extension/extension.js`
- [ ] `rg -n 'Content-Security-Policy|cspSource|nonce-' V2/cursor-extension/extension.js`
- [ ] `! rg -n 'cdnjs|font-awesome|https://cdnjs|innerHTML\\s*=|onclick=' V2/cursor-extension/extension.js`
- [ ] Confirm image previews, drag-drop, paste upload, and speech actions still use DOM-safe handlers after the HTML refactor.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The popup now loads assets locally, enforces a strict CSP, and avoids unsafe HTML injection patterns, satisfying SEC-03 without changing the core Review Gate UX.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoped-ipc-security/03-03-SUMMARY.md`
</output>
