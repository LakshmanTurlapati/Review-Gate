---
phase: 01-installation-integrity
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - V2/install.sh
  - V2/install.ps1
  - V2/install.bat
  - V2/uninstall.sh
  - V2/uninstall.ps1
  - V2/uninstall.bat
autonomous: true
requirements:
  - INST-03
  - INST-04
user_setup: []
must_haves:
  truths:
    - "Every installer uses a VSIX filename that actually exists in `V2/` for the current shipped version."
    - "Every installer and uninstaller copies or removes the current V2 rule file `ReviewGateV2.mdc`, not the legacy root rule."
    - "POSIX uninstall removes only `review-gate-v2` from Cursor MCP config instead of wiping the whole file."
  artifacts:
    - path: V2/install.sh
      provides: "POSIX installer with current VSIX lookup and current V2 rule install path."
      contains: "ReviewGateV2.mdc"
    - path: V2/install.ps1
      provides: "PowerShell installer with current VSIX lookup and current V2 rule install path."
      contains: "ReviewGateV2.mdc"
    - path: V2/install.bat
      provides: "Batch installer with current VSIX lookup and current V2 rule install path."
      contains: "ReviewGateV2.mdc"
    - path: V2/uninstall.sh
      provides: "POSIX uninstaller that removes only Review Gate's MCP entry and current V2 rule file."
      contains: "update_mcp_config.py"
    - path: V2/uninstall.ps1
      provides: "PowerShell uninstaller that removes the current V2 rule file by its correct name."
      contains: "ReviewGateV2.mdc"
    - path: V2/uninstall.bat
      provides: "Batch uninstaller that removes the current V2 rule file by its correct name."
      contains: "ReviewGateV2.mdc"
  key_links:
    - from: V2/cursor-extension/package.json
      to: V2/install.sh
      via: "current versioned VSIX basename"
      pattern: "review-gate-v2-2.7.3.vsix"
    - from: V2/cursor-extension/package.json
      to: V2/install.ps1
      via: "current versioned VSIX basename"
      pattern: "review-gate-v2-2.7.3.vsix"
    - from: V2/cursor-extension/package.json
      to: V2/install.bat
      via: "current versioned VSIX basename"
      pattern: "review-gate-v2-2.7.3.vsix"
    - from: V2/ReviewGateV2.mdc
      to: V2/install.ps1
      via: "rule copy source path"
      pattern: "ReviewGateV2.mdc"
    - from: V2/ReviewGateV2.mdc
      to: V2/install.sh
      via: "rule copy source path"
      pattern: "ReviewGateV2.mdc"
    - from: V2/ReviewGateV2.mdc
      to: V2/install.bat
      via: "rule copy source path"
      pattern: "ReviewGateV2.mdc"
    - from: V2/ReviewGateV2.mdc
      to: V2/uninstall.ps1
      via: "rule removal target path"
      pattern: "ReviewGateV2.mdc"
    - from: V2/ReviewGateV2.mdc
      to: V2/uninstall.bat
      via: "rule removal target path"
      pattern: "ReviewGateV2.mdc"
    - from: V2/uninstall.sh
      to: V2/update_mcp_config.py
      via: "remove invocation for `~/.cursor/mcp.json`"
      pattern: "update_mcp_config.py.*remove"
---

<objective>
Align every installer and uninstaller with the actual shipped V2 artifact names and safe cleanup behavior.

Purpose: Make the runtime scripts point at the files that really exist in the repo and finish the config-preservation work on the POSIX uninstall path.
Output: Updated install/uninstall scripts that resolve the current VSIX, use `ReviewGateV2.mdc`, and preserve unrelated MCP config during uninstall.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installation-integrity/01-CONTEXT.md
@V2/cursor-extension/package.json
@V2/ReviewGateV2.mdc
@V2/install.sh
@V2/install.ps1
@V2/install.bat
@V2/uninstall.sh
@V2/uninstall.ps1
@V2/uninstall.bat
@V2/update_mcp_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize installer VSIX lookup to the shipped 2.7.3 artifact set</name>
  <files>V2/install.sh, V2/install.ps1, V2/install.bat</files>
  <read_first>V2/cursor-extension/package.json, V2/install.sh, V2/install.ps1, V2/install.bat</read_first>
  <action>In all three installers, stop assuming a single hardcoded VSIX location. Read or define the current extension version from `V2/cursor-extension/package.json` as `2.7.3`, construct the basename `review-gate-v2-2.7.3.vsix`, and resolve it in this exact order: `V2/review-gate-v2-2.7.3.vsix` first, then `V2/cursor-extension/review-gate-v2-2.7.3.vsix` as the fallback build-output location. Copy the resolved file into the install directory under the same basename and update every manual-install prompt and installation summary line to point at that copied basename. Keep installation failing with a clear error only when neither shipped file exists.</action>
  <verify>python3 - <<'PY'
import json
import pathlib

version = json.loads(pathlib.Path('V2/cursor-extension/package.json').read_text(encoding='utf-8'))['version']
expected = f'review-gate-v2-{version}.vsix'
for rel in ['V2/install.sh', 'V2/install.ps1', 'V2/install.bat']:
    text = pathlib.Path(rel).read_text(encoding='utf-8')
    assert expected in text, rel
    assert 'cursor-extension' in text, rel
    assert 'review-gate-v2-2.6.4.vsix' not in text, rel
print('ok')
PY</verify>
  <acceptance_criteria>
    - Each installer references the current versioned basename `review-gate-v2-2.7.3.vsix`.
    - Each installer can resolve either the root `V2/` VSIX or the `V2/cursor-extension/` fallback without changing the copied filename.
  </acceptance_criteria>
  <done>All installers use the current shipped VSIX paths instead of stale or single-location assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Replace legacy rule handling with `ReviewGateV2.mdc` everywhere</name>
  <files>V2/install.sh, V2/install.ps1, V2/install.bat, V2/uninstall.sh, V2/uninstall.ps1, V2/uninstall.bat</files>
  <read_first>V2/ReviewGateV2.mdc, V2/install.sh, V2/install.ps1, V2/install.bat, V2/uninstall.sh, V2/uninstall.ps1, V2/uninstall.bat</read_first>
  <action>Replace every `ReviewGate.mdc` reference in the V2 installers and uninstallers with `ReviewGateV2.mdc`. The installers must copy `V2/ReviewGateV2.mdc` into the appropriate Cursor rules directory using the filename `ReviewGateV2.mdc`, and the uninstallers must remove that exact filename from the same rules directory. Update installation summaries, manual follow-up steps, and uninstall summaries in those scripts so they report `ReviewGateV2.mdc` explicitly. Do not edit or delete the legacy repo-root `ReviewGate.mdc` file in this phase.</action>
  <verify>! rg -n 'ReviewGate\\.mdc' V2/install.sh V2/install.ps1 V2/install.bat V2/uninstall.sh V2/uninstall.ps1 V2/uninstall.bat && rg -n 'ReviewGateV2\\.mdc' V2/install.sh V2/install.ps1 V2/install.bat V2/uninstall.sh V2/uninstall.ps1 V2/uninstall.bat</verify>
  <acceptance_criteria>
    - No V2 installer or uninstaller references `ReviewGate.mdc`.
    - Every V2 installer and uninstaller references `ReviewGateV2.mdc`.
  </acceptance_criteria>
  <done>The runtime scripts install and remove the current V2 rule file instead of the legacy rule name.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve unrelated MCP config during POSIX uninstall</name>
  <files>V2/uninstall.sh</files>
  <read_first>V2/update_mcp_config.py, V2/uninstall.sh</read_first>
  <action>Replace the destructive line in `V2/uninstall.sh` that writes `{\"mcpServers\":{}}` with a call to `python3 "$SCRIPT_DIR/update_mcp_config.py" remove --config "$HOME/.cursor/mcp.json" --server-name review-gate-v2`. Keep the existing backup creation behavior, restore the backup if helper execution fails, and exit non-zero on failure. Do not change the extension uninstall or temp-file cleanup logic in this task.</action>
  <verify>! rg -n '\\{\"mcpServers\":\\{\\}\\}' V2/uninstall.sh && rg -n 'update_mcp_config.py.*remove' V2/uninstall.sh</verify>
  <acceptance_criteria>
    - `V2/uninstall.sh` no longer rewrites `~/.cursor/mcp.json` to an empty `mcpServers` object.
    - `V2/uninstall.sh` invokes `update_mcp_config.py remove` for targeted cleanup.
  </acceptance_criteria>
  <done>POSIX uninstall removes only Review Gate's MCP entry and leaves unrelated user MCP configuration untouched.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The Python version/VSIX check in Task 1 passes
- [ ] `! rg -n 'ReviewGate\\.mdc|2\\.6\\.4' V2/install.sh V2/install.ps1 V2/install.bat V2/uninstall.sh V2/uninstall.ps1 V2/uninstall.bat`
- [ ] `! rg -n '\\{\"mcpServers\":\\{\\}\\}' V2/uninstall.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime install/uninstall scripts reference the current shipped VSIX and V2 rule file while preserving unrelated MCP config during cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-installation-integrity/01-03-SUMMARY.md`
</output>
