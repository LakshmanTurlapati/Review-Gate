---
phase: 01-installation-integrity
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - V2/INSTALLATION.md
  - readme.md
autonomous: true
requirements:
  - INST-04
user_setup: []
must_haves:
  truths:
    - "The detailed installation guide tells users to install the actual shipped VSIX and current V2 rule file."
    - "The top-level README describes the same current install surface as the scripts: `review-gate-v2-2.7.3.vsix`, `ReviewGateV2.mdc`, and MCP config preservation."
  artifacts:
    - path: V2/INSTALLATION.md
      provides: "Detailed manual and automated install instructions that match the shipped V2 files."
      contains: "review-gate-v2-2.7.3.vsix"
    - path: readme.md
      provides: "Top-level installation guidance aligned with Phase 01 runtime behavior."
      contains: "ReviewGateV2.mdc"
  key_links:
    - from: V2/INSTALLATION.md
      to: V2/ReviewGateV2.mdc
      via: "manual rule-copy instructions"
      pattern: "ReviewGateV2.mdc"
    - from: readme.md
      to: V2/INSTALLATION.md
      via: "same current artifact names and merge-safe MCP guidance"
      pattern: "review-gate-v2-2.7.3.vsix|ReviewGateV2.mdc"
---

<objective>
Align the user-facing installation documentation with the corrected Phase 01 runtime behavior and shipped V2 files.

Purpose: Finish Phase 01 by removing stale filenames and wrong-path instructions after the scripts are fixed.
Output: Updated `V2/INSTALLATION.md` and `readme.md` that describe the current 2.7.3 VSIX, `ReviewGateV2.mdc`, and config-preserving MCP setup.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installation-integrity/01-CONTEXT.md
@V2/INSTALLATION.md
@readme.md
@V2/cursor-extension/package.json
@V2/ReviewGateV2.mdc
@V2/mcp.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the detailed installation guide to match shipped files and merge-safe config behavior</name>
  <files>V2/INSTALLATION.md</files>
  <read_first>V2/INSTALLATION.md, V2/cursor-extension/package.json, V2/ReviewGateV2.mdc, V2/mcp.json</read_first>
  <action>Rewrite the stale install references in `V2/INSTALLATION.md` so the document uses the current extension version `2.7.3`, the current rule file `V2/ReviewGateV2.mdc`, and the actual repo layout under `V2/`. Replace every `review-gate-v2-2.6.4.vsix` mention with `review-gate-v2-2.7.3.vsix`, call out `V2/review-gate-v2-2.7.3.vsix` as the primary shipped artifact with `V2/cursor-extension/review-gate-v2-2.7.3.vsix` as the fallback build-output location, and tell manual users to merge the `review-gate-v2` entry into existing `mcpServers` instead of replacing the whole file. Keep the guide scoped to the current V2 flow; do not add future release-process cleanup from Phase 5.</action>
  <verify>! rg -n '2\\.6\\.4|ReviewGate\\.mdc' V2/INSTALLATION.md && rg -n 'review-gate-v2-2.7.3\\.vsix|ReviewGateV2\\.mdc|merge.*mcpServers|preserv' V2/INSTALLATION.md</verify>
  <acceptance_criteria>
    - `V2/INSTALLATION.md` has no remaining `2.6.4` or `ReviewGate.mdc` references.
    - `V2/INSTALLATION.md` explicitly tells users to preserve existing `mcpServers` entries while adding `review-gate-v2`.
  </acceptance_criteria>
  <done>The detailed installation guide matches the shipped 2.7.3 artifact names and current V2 rule path.</done>
</task>

<task type="auto">
  <name>Task 2: Update the top-level README installation section to match the corrected V2 flow</name>
  <files>readme.md</files>
  <read_first>readme.md, V2/INSTALLATION.md, V2/cursor-extension/package.json, V2/ReviewGateV2.mdc</read_first>
  <action>Update the V2 installation section in `readme.md` so it matches the corrected scripts and `V2/INSTALLATION.md`. The README must reference `review-gate-v2-2.7.3.vsix`, `V2/ReviewGateV2.mdc`, and the fact that installers preserve unrelated MCP server entries instead of replacing user configuration. Keep the existing V1 legacy section unless a line directly contradicts the corrected V2 instructions. Do not add new distribution channels or marketplace claims in this phase.</action>
  <verify>! rg -n '2\\.6\\.4|ReviewGate\\.mdc' readme.md && rg -n 'review-gate-v2-2.7.3\\.vsix|ReviewGateV2\\.mdc|preserve.*MCP|merge.*configuration' readme.md</verify>
  <acceptance_criteria>
    - `readme.md` references the same current VSIX and rule file as `V2/INSTALLATION.md`.
    - `readme.md` states that the installer preserves existing MCP configuration.
  </acceptance_criteria>
  <done>The top-level installation instructions no longer send users to stale filenames or incorrect rule paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `! rg -n '2\\.6\\.4|ReviewGate\\.mdc' V2/INSTALLATION.md readme.md`
- [ ] `rg -n 'review-gate-v2-2.7.3\\.vsix|ReviewGateV2\\.mdc' V2/INSTALLATION.md readme.md`
- [ ] Confirm both docs mention preserving existing MCP config instead of replacing it.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- User-facing install docs match the corrected Phase 01 runtime behavior and shipped repository contents.
</success_criteria>

<output>
After completion, create `.planning/phases/01-installation-integrity/01-04-SUMMARY.md`
</output>
