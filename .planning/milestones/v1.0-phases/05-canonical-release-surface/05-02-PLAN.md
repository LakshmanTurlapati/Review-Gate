---
phase: 05-canonical-release-surface
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - V2/install.sh
  - V2/install.ps1
  - V2/install.bat
  - tests/smoke/test_installers.py
autonomous: true
requirements:
  - REL-01
user_setup: []
must_haves:
  truths:
    - "All supported installers resolve the same canonical VSIX basename and rule file from shared release metadata instead of hardcoded duplicate strings."
    - "Installers fail clearly when the canonical `V2/` VSIX is missing rather than treating `V2/cursor-extension/` as a second supported release source."
    - "Installer smoke coverage proves the narrowed release surface in temp roots before maintainers ship a release."
  artifacts:
    - path: V2/install.sh
      provides: "POSIX installer that reads canonical release metadata and installs only the root `V2/` VSIX."
      contains: "package_review_gate_vsix.py"
    - path: V2/install.ps1
      provides: "PowerShell installer aligned to the canonical manifest-driven VSIX and rule contract."
      contains: "package_review_gate_vsix.py"
    - path: V2/install.bat
      provides: "Batch installer aligned to the same canonical manifest-driven VSIX and rule contract."
      contains: "package_review_gate_vsix.py"
    - path: tests/smoke/test_installers.py
      provides: "Installer smoke coverage that asserts root-only VSIX resolution and clear failure when the canonical artifact is missing."
      contains: "REVIEW_GATE_SMOKE"
  key_links:
    - from: V2/release-manifest.json
      to: V2/install.sh
      via: "installer shells out to the 05-01 helper for version, artifact basename, and rule path"
      pattern: "package_review_gate_vsix.py|ReviewGateV2\\.mdc"
    - from: V2/release-manifest.json
      to: V2/install.ps1
      via: "installer shells out to the 05-01 helper for version, artifact basename, and rule path"
      pattern: "package_review_gate_vsix.py|ReviewGateV2\\.mdc"
    - from: V2/release-manifest.json
      to: V2/install.bat
      via: "installer shells out to the 05-01 helper for version, artifact basename, and rule path"
      pattern: "package_review_gate_vsix.py|ReviewGateV2\\.mdc"
    - from: tests/smoke/test_installers.py
      to: V2/install.sh
      via: "smoke-mode temp roots assert the canonical artifact lookup and copied install asset"
      pattern: "REVIEW_GATE_SMOKE|review-gate-v2-2\\.7\\.3\\.vsix"
---

<objective>
Make every supported installer consume the canonical release surface from Plan 05-01.

Purpose: A single committed VSIX path is not enough if install scripts still encode a second supported artifact path or version strings independently. This plan narrows runtime resolution to the canonical `V2/` artifact and proves it through smoke coverage.
Output: Installers that read shared release metadata, resolve only the canonical root VSIX, and smoke tests that assert the narrowed install surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-canonical-release-surface/05-CONTEXT.md
@.planning/phases/01-installation-integrity/01-03-SUMMARY.md
@.planning/phases/04-automated-hardening-verification/04-03-SUMMARY.md
@V2/release-manifest.json
@scripts/package_review_gate_vsix.py
@V2/install.sh
@V2/install.ps1
@V2/install.bat
@tests/smoke/test_installers.py

<interfaces>
Expected from `V2/release-manifest.json` after 05-01:
```json
{
  "version": "2.7.3",
  "artifact_basename": "review-gate-v2-2.7.3.vsix",
  "canonical_vsix_path": "V2/review-gate-v2-2.7.3.vsix",
  "rule_path": "V2/ReviewGateV2.mdc",
  "extension_source_dir": "V2/cursor-extension"
}
```

Expected helper CLI from `scripts/package_review_gate_vsix.py` after 05-01:
```bash
python3 scripts/package_review_gate_vsix.py --field version
python3 scripts/package_review_gate_vsix.py --field artifact_basename
python3 scripts/package_review_gate_vsix.py --field canonical_vsix_path
python3 scripts/package_review_gate_vsix.py --field rule_path
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire every installer to the canonical manifest-driven artifact contract</name>
  <files>V2/install.sh, V2/install.ps1, V2/install.bat</files>
  <read_first>V2/release-manifest.json, scripts/package_review_gate_vsix.py, V2/install.sh, V2/install.ps1, V2/install.bat, .planning/phases/01-installation-integrity/01-03-SUMMARY.md</read_first>
  <action>Replace the duplicated installer literals for version, VSIX basename, and rule filename with one shared metadata lookup from the 05-01 helper or manifest. Each installer must resolve the canonical root artifact `V2/review-gate-v2-<version>.vsix`, keep the installed basename stable inside the user install directory, and keep existing smoke-mode, MCP merge, and rule-copy behavior intact. Remove the `V2/cursor-extension/` fallback path and make missing-canonical-artifact errors explicit. Do not build the VSIX during install, do not reintroduce the duplicate artifact path as a fallback, and do not change the supported rule file away from `V2/ReviewGateV2.mdc`.</action>
  <verify>rg -n 'package_review_gate_vsix\\.py|ReviewGateV2\\.mdc' V2/install.sh V2/install.ps1 V2/install.bat && ! rg -n 'cursor-extension[\\\\/].*review-gate-v2-.*\\.vsix' V2/install.sh V2/install.ps1 V2/install.bat</verify>
  <acceptance_criteria>
    - All three installers resolve release metadata from the shared Phase 5 source of truth.
    - None of the installers describe `V2/cursor-extension/` as a supported VSIX source anymore.
  </acceptance_criteria>
  <done>Cross-platform install flows use one canonical release contract instead of per-script artifact drift.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten installer smoke coverage around the root-only release artifact</name>
  <files>tests/smoke/test_installers.py</files>
  <read_first>tests/smoke/test_installers.py, V2/install.sh, V2/install.ps1, V2/install.bat, .planning/phases/04-automated-hardening-verification/04-03-SUMMARY.md</read_first>
  <action>Update `tests/smoke/test_installers.py` so the smoke harness stages and asserts only the canonical root `V2/review-gate-v2-2.7.3.vsix` artifact. Cover two cases: 1) smoke installs copy the canonical artifact into the temp install root and preserve `ReviewGateV2.mdc` plus merged MCP config, and 2) missing-canonical-artifact runs fail with a clear error instead of silently falling back to `V2/cursor-extension/`. Keep the existing shell-availability skips explicit on machines without PowerShell or CMD. Do not add CI-only test wrappers or replace the existing smoke-mode contract.</action>
  <verify>python3 -m unittest tests/smoke/test_installers.py -v</verify>
  <acceptance_criteria>
    - Smoke tests pass when the canonical root artifact is present.
    - Smoke tests prove a missing canonical artifact is a hard failure, not a fallback to a second repo path.
  </acceptance_criteria>
  <done>Installer automation now proves the narrowed release surface before docs and final release gating are updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'package_review_gate_vsix\\.py|ReviewGateV2\\.mdc' V2/install.sh V2/install.ps1 V2/install.bat`
- [ ] `! rg -n 'cursor-extension[\\\\/].*review-gate-v2-.*\\.vsix' V2/install.sh V2/install.ps1 V2/install.bat`
- [ ] `python3 -m unittest tests/smoke/test_installers.py -v`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Cross-platform installers consume one canonical artifact contract from Phase 5 metadata
- Smoke coverage proves there is no second supported VSIX path hiding behind installer fallback logic
</success_criteria>

<output>
After completion, create `.planning/phases/05-canonical-release-surface/05-02-SUMMARY.md`
</output>
